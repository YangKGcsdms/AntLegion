🌐 [English](PROTOCOL.md) · **简体中文导读**

# AntLegion 协议 —— v2.0(中文导读)

> 一个本原。一次写。一次读。其余一切皆由其推导。
>
> 由 **Carter.Yang** 设计,2026 年从第一性原理重新推导。

> ⚠️ **本文为中文导读。** 完整、规范性(MUST/SHOULD/MAY)的条款以英文
> [`PROTOCOL.md`](PROTOCOL.md) 为准——字段表、折叠伪代码、边界规则等以英文原文为权威。
> 本文忠实翻译概念核心(§0 推导)并给出分节索引,供中文读者快速建立全局认识。

---

## 0. 推导(v2 为何如此)

### 0.1 唯一本原(一元论)

系统里只存在一种东西:

> **一条 Fact:不可变、内容寻址的陈述,置于单一全序中的一个唯一位置。**

这就是全部本体论。没有独立的「任务」「认领」「投票」「信任级别」「状态」——那些词只是
*事实的模式*;事实是一切。这是一元论的一步,下面每条规则都是它的*推论*,而非附加。

只有两个操作作用于本原,仅此两个:

- **append(fact) → seq** —— 追加一条事实;总线为它分配全序中的下一个位置。
- **read(since_seq) → fact[]** —— 返回某位置之后的事实,按序。

### 0.2 总线是什么(不是什么)

由本原出发,总线 MUST 提供的可信内核又小又固定:

1. **有序** —— 分配严格递增的 `seq`。这个全序是总线*唯一*的权威。
2. **完整性** —— 校验 `id == hash(record)`;不符则拒绝。
3. **持久** —— 追加到一个能跨重启存活的日志。
4. **区间读** —— 按序返回 `seq > since`。

总线**没有 per-fact 可变状态**,没有状态机、没有认领表、没有信任计算、没有派发、没有
仲裁、没有推送。它是一条*可验证、全序、只追加的日志*——能在其上推导出其余一切的最小对象。
(类比:一个签名的 Kafka 单分区,或带序号的 git。)

> **不裁决公理。** 总线为事实排序并保存。它从不裁决它们的*含义*。含义由读者计算(§3)。
> 这正是信任、生命周期、独占性都是读者折叠、而非服务端状态的原因。

### 0.3 其余一切,皆由推导

| v1 概念 | v2 推导 |
|---|---|
| 工作流状态(`published/claimed/resolved/dead`) | 对引用目标的 claim/resolve/tombstone 事实做**折叠** |
| `epistemic_state` + quorum 配置 | 对 `vote` 事实做**折叠**;quorum 是*读者*的策略 |
| 原子 `claim` 端点 + 仲裁 | 追加一条 `claim` 事实;**引用目标的 seq 最小者获胜**——恰好一次是全序的定理 |
| `supersedes` / 自动取代索引 | 事实携带 `supersedes`;读者按 subject 保留 seq 最高者 |
| `causation_chain` + `causation_depth` | 走 `parent` 链;深度是算出来的,不存 |
| TTL → `dead` 转换 | 读者侧过滤 / 压缩提示;绝非服务端状态变更 |
| 接受过滤 / 派发 | 读者侧查询谓词(服务端可作为纯读优化提供) |
| 事件推送 / WebSocket | 移除;读取推进游标 |
| `semantic_kind`、`schema_version`、`priority`、`confidence`、TEC、reliability | 可选的 payload/ref 提示;可信内核**一概不解释** |

事实的线上形态从约 30 个字段缩到一小撮(§1)。两套服务端状态机消失。「智能」逻辑移入
每种语言只写一次的**客户端折叠库**(§3),而非在每个总线实现里重写。

**优雅去了哪。** v1 的 MCP 适配器靠*转发*保持优雅(调 `/claim`,拿 200/409)。v2 下适配器
必须*折叠*(追加一条 claim、读回确认、投影出 state/trust)。客户端表面(如 6 个 MCP 工具)
可以保持一样简单,但这只因为适配器/折叠库现在吸收了这份活。v2 让**总线**变简单、让**适配器**
略变重;跳过适配器的裸客户端则用「一次往返」换成「追加 + 折叠」。这是把复杂度**刻意搬到**
唯一能写一次、又能被一致性测试覆盖的地方,而非删除。

---

## 分节索引(完整条款见英文 [`PROTOCOL.md`](PROTOCOL.md))

- **§1 The Fact** —— 事实的 8 个字段(`seq, recv, id, type, author, ts, payload, refs, nonce?, sig`)与 `refs` 关系键(`parent/claim_of/resolves/release_of/vote/supersedes/subject/tombstones`)。关键:`ts` 是作者自报(仅参考、可伪造),`recv` 是总线盖的可信时间;**一切基于时间的折叠用 `recv`,绝不用 `ts`**。
- **§2 Bus operations** —— 全部线面:`POST /facts`(写,按 id 幂等)、`GET /facts?since=…`(读,游标式)、`/facts/head`、`/facts/:id`。
- **§3 Reader folds(规范性)** —— 意义所在:§3.1 生命周期(活跃认领集 + recv 锚定的确定性超时;resolved/dead 为终态)、§3.2 信任(对 vote 折叠;忽略自投、只计每位作者最新一票;superseded 压倒一切)、§3.3 取代(同 subject latest-wins)、§3.4 因果(走 parent;tombstone 保骨架故链不断)。
- **§4 Identity & integrity** —— `id = sha256(canonical)`;按 id 幂等(重发返回既有,换 `nonce` 得新事实);`sig = hmac(secret, "id|author|type|ts|recv|seq")`;跨语言一致性向量是互操作契约。
- **§5 安全规则** —— 内核只强制:完整性、因果环/深度上限、(可选)限流。§5.2 删除即事实:tombstone 用专属 `refs.tombstones`,压缩只丢 payload、保留 `{id,seq,recv,author,refs,sig}` 骨架。
- **§6 鉴权** —— 属传输层、可选(mTLS/网关/API key);公网部署 SHOULD 认证 `author`。
- **§7 存储与恢复** —— 只追加日志即状态,崩溃恢复=截断到最后合法字节;**全序 ⇒ 单一逻辑写入点**,高可用=单写+故障切换,非多主。
- **§8 默认值** —— 因果深度上限 64、读者 quorum 2、认领超时 600s、限流 20 突发/5 每秒等。
- **§9 v1 → v2 映射** —— 旧端点到「追加保留类型事实(`_.claim`/`_.resolve`/`_.vote`)+ 折叠」的对照;可用一个读者 shim 重新暴露 v1 REST 给老客户端。
- **§10 血统** —— 事件溯源/CQRS、git、Lamport 全序、CAN 总线、科学方法。

---

*v2.0 协议由 Carter.Yang 设计。总线排序并保存,读者裁决含义。把可信内核保持得足够小,
让第二个实现是一个周末的事,并让一致性向量——而非散文——来保证互操作。*
