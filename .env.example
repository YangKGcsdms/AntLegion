# AntLegion Bus configuration
# Used to sign facts. Set to any stable random string in production.
# If unset, the bus generates a fresh secret on each boot (fine for development,
# but existing fact signatures cannot be re-verified after a restart).
ANTLEGION_BUS_SECRET=change-this-to-any-random-string
