{
  "name": "@antlegion/mcp-server",
  "version": "0.1.0",
  "description": "MCP server for AntLegion Bus — universal adapter for Claude Code / Cursor / Cline / Continue / any MCP client",
  "license": "MIT",
  "type": "module",
  "bin": {
    "antlegion-mcp": "dist/index.js"
  },
  "main": "dist/index.js",
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/index.js",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.18.0"
  },
  "devDependencies": {
    "@types/node": "^22",
    "typescript": "^5.7"
  },
  "engines": {
    "node": ">=20"
  }
}
